# Disabled — app uses deploy.yml (Vite/React build)
name: Jekyll (disabled)
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy.yml instead"
